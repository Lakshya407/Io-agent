[pytest]
asyncio_mode = auto
# A single session-scoped event loop: the SQLAlchemy async engine and its
# asyncpg connections bind to one loop, so per-test loops would orphan them.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
