# Production configuration used by the Docker/Nginx build.
#
# Nginx serves the built assets and reverse-proxies /api and /health to the
# backend container, so the browser uses a same-origin relative path. This
# removes cross-origin requests entirely in production.
VITE_API_BASE_URL=/api/v1
